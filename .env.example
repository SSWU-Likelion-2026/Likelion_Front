FIGMA_TOKEN=
FIGMA_FILE_KEY=SEn5vGMB7vontn8qmKCswa

# 인증 API 연동
# (A) dev 프록시로 CORS 회피 — /api 상대경로 호출이 아래 주소로 전달됨
VITE_API_PROXY_TARGET=http://52.79.239.27:8080
# (B) 또는 절대 URL 로 바로 호출 (A 안 쓸 때)
VITE_API_BASE_URL=

# 구글 로그인 웹 클라이언트 ID (Google Cloud Console → 사용자 인증 정보)
# 미설정 시 "Sign in with Google" 버튼은 비활성 상태로 표시됨
VITE_GOOGLE_CLIENT_ID=
